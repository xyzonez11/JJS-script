-- JJK Chase GUI - TELEPORT sau lưng
local Player = game.Players.LocalPlayer
local UserInputService = game.UserInputService

local isActive = false
local targetPlayer = nil
local gui = nil
local dragging = false
local dragStart = nil
local startPos = nil
local connection = nil

-- Tạo GUI
local function createGUI()
    gui = Instance.new("ScreenGui")
    gui.Name = "JJKChase"
    gui.Parent = game.CoreGui
    gui.ResetOnSpawn = false
    gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    
    local main = Instance.new("Frame")
    main.Name = "Main"
    main.Size = UDim2.new(0, 300, 0, 350)
    main.Position = UDim2.new(0, 100, 0, 100)
    main.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    main.BorderSizePixel = 0
    main.Visible = false
    main.Active = true
    main.Parent = gui
    
    Instance.new("UICorner", main).CornerRadius = UDim.new(0, 10)
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(138, 43, 226)
    stroke.Thickness = 1.5
    stroke.Parent = main
    
    -- Header
    local header = Instance.new("Frame")
    header.Name = "Header"
    header.Size = UDim2.new(1, 0, 0, 40)
    header.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
    header.BackgroundTransparency = 0.7
    header.BorderSizePixel = 0
    header.Active = true
    header.Parent = main
    
    Instance.new("UICorner", header).CornerRadius = UDim.new(0, 10)
    
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -50, 1, 0)
    title.Position = UDim2.new(0, 15, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = "👁️ Six Eyes Chase"
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.Font = Enum.Font.GothamBold
    title.TextSize = 14
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = header
    
    local close = Instance.new("TextButton")
    close.Size = UDim2.new(0, 25, 0, 25)
    close.Position = UDim2.new(1, -35, 0.5, -12)
    close.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
    close.BackgroundTransparency = 0.6
    close.Text = "✕"
    close.TextColor3 = Color3.fromRGB(255, 255, 255)
    close.Font = Enum.Font.GothamBold
    close.TextSize = 14
    close.Parent = header
    
    Instance.new("UICorner", close).CornerRadius = UDim.new(1, 0)
    
    close.MouseButton1Click:Connect(function()
        main.Visible = false
    end)
    
    -- DRAG
    header.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = main.Position
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)
    
    -- Selected target display
    local selFrame = Instance.new("Frame")
    selFrame.Size = UDim2.new(1, -30, 0, 35)
    selFrame.Position = UDim2.new(0, 15, 0, 50)
    selFrame.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
    selFrame.BackgroundTransparency = 0.8
    selFrame.BorderSizePixel = 0
    selFrame.Parent = main
    
    Instance.new("UICorner", selFrame).CornerRadius = UDim.new(0, 6)
    
    local selLabel = Instance.new("TextLabel")
    selLabel.Size = UDim2.new(0, 80, 1, 0)
    selLabel.Position = UDim2.new(0, 8, 0, 0)
    selLabel.BackgroundTransparency = 1
    selLabel.Text = "🎯 Mục tiêu:"
    selLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    selLabel.Font = Enum.Font.GothamSemibold
    selLabel.TextSize = 11
    selLabel.TextXAlignment = Enum.TextXAlignment.Left
    selLabel.Parent = selFrame
    
    local selName = Instance.new("TextLabel")
    selName.Name = "SelectedName"
    selName.Size = UDim2.new(1, -90, 1, 0)
    selName.Position = UDim2.new(0, 85, 0, 0)
    selName.BackgroundTransparency = 1
    selName.Text = "Chưa chọn"
    selName.TextColor3 = Color3.fromRGB(255, 255, 255)
    selName.Font = Enum.Font.GothamBold
    selName.TextSize = 12
    selName.TextXAlignment = Enum.TextXAlignment.Left
    selName.Parent = selFrame
    
    -- List label
    local listLabel = Instance.new("TextLabel")
    listLabel.Size = UDim2.new(1, -30, 0, 18)
    listLabel.Position = UDim2.new(0, 15, 0, 92)
    listLabel.BackgroundTransparency = 1
    listLabel.Text = "📋 Danh sách người chơi"
    listLabel.TextColor3 = Color3.fromRGB(170, 170, 255)
    listLabel.Font = Enum.Font.GothamSemibold
    listLabel.TextSize = 10
    listLabel.TextXAlignment = Enum.TextXAlignment.Left
    listLabel.Parent = main
    
    -- Player list
    local scroll = Instance.new("ScrollingFrame")
    scroll.Name = "PlayerScroll"
    scroll.Size = UDim2.new(1, -30, 0, 120)
    scroll.Position = UDim2.new(0, 15, 0, 112)
    scroll.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    scroll.BackgroundTransparency = 0.7
    scroll.BorderSizePixel = 0
    scroll.ScrollBarThickness = 3
    scroll.ScrollBarImageColor3 = Color3.fromRGB(138, 43, 226)
    scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    scroll.Parent = main
    
    Instance.new("UICorner", scroll).CornerRadius = UDim.new(0, 6)
    
    local listLayout = Instance.new("UIListLayout")
    listLayout.Padding = UDim.new(0, 2)
    listLayout.Parent = scroll
    
    -- Refresh
    local refreshBtn = Instance.new("TextButton")
    refreshBtn.Size = UDim2.new(1, -30, 0, 30)
    refreshBtn.Position = UDim2.new(0, 15, 0, 238)
    refreshBtn.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
    refreshBtn.BackgroundTransparency = 0.8
    refreshBtn.Text = "🔄 Làm mới danh sách"
    refreshBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    refreshBtn.Font = Enum.Font.GothamSemibold
    refreshBtn.TextSize = 11
    refreshBtn.Parent = main
    
    Instance.new("UICorner", refreshBtn).CornerRadius = UDim.new(0, 6)
    
    refreshBtn.MouseButton1Click:Connect(function()
        updatePlayerList()
    end)
    
    -- Status
    local statusFrame = Instance.new("Frame")
    statusFrame.Size = UDim2.new(1, -30, 0, 35)
    statusFrame.Position = UDim2.new(0, 15, 0, 275)
    statusFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    statusFrame.BackgroundTransparency = 0.6
    statusFrame.BorderSizePixel = 0
    statusFrame.Parent = main
    
    Instance.new("UICorner", statusFrame).CornerRadius = UDim.new(0, 6)
    
    local dot = Instance.new("Frame")
    dot.Name = "StatusDot"
    dot.Size = UDim2.new(0, 10, 0, 10)
    dot.Position = UDim2.new(0, 10, 0.5, -5)
    dot.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
    dot.Parent = statusFrame
    
    Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)
    
    local statusText = Instance.new("TextLabel")
    statusText.Name = "StatusText"
    statusText.Size = UDim2.new(1, -30, 1, 0)
    statusText.Position = UDim2.new(0, 25, 0, 0)
    statusText.BackgroundTransparency = 1
    statusText.Text = "Chưa kích hoạt"
    statusText.TextColor3 = Color3.fromRGB(220, 220, 220)
    statusText.Font = Enum.Font.Gotham
    statusText.TextSize = 11
    statusText.TextXAlignment = Enum.TextXAlignment.Left
    statusText.Parent = statusFrame
    
    -- Toggle
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Name = "ToggleBtn"
    toggleBtn.Size = UDim2.new(1, -30, 0, 35)
    toggleBtn.Position = UDim2.new(0, 15, 0, 315)
    toggleBtn.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
    toggleBtn.Text = "▶ KÍCH HOẠT"
    toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.TextSize = 13
    toggleBtn.AutoButtonColor = false
    toggleBtn.Parent = main
    
    Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(0, 8)
    
    toggleBtn.MouseButton1Click:Connect(function()
        toggleChase()
    end)
    
    return main
end

function updatePlayerList()
    local scroll = gui.Main.PlayerScroll
    for _, child in pairs(scroll:GetChildren()) do
        if child:IsA("TextButton") then
            child:Destroy()
        end
    end
    
    local players = game.Players:GetPlayers()
    local count = 0
    
    for _, p in pairs(players) do
        if p ~= Player then
            count = count + 1
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(1, 0, 0, 28)
            btn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            btn.BackgroundTransparency = 0.95
            btn.Text = "  " .. p.DisplayName
            btn.TextColor3 = Color3.fromRGB(200, 200, 200)
            btn.Font = Enum.Font.Gotham
            btn.TextSize = 12
            btn.TextXAlignment = Enum.TextXAlignment.Left
            btn.AutoButtonColor = false
            btn.Parent = scroll
            
            btn.MouseButton1Click:Connect(function()
                selectPlayer(p)
            end)
        end
    end
    
    scroll.CanvasSize = UDim2.new(0, 0, 0, count * 30)
end

function selectPlayer(p)
    local selName = gui.Main:FindFirstChild("SelectedName", true)
    if selName then selName.Text = p.Name end
    
    local scroll = gui.Main.PlayerScroll
    for _, btn in pairs(scroll:GetChildren()) do
        if btn:IsA("TextButton") then
            if btn.Text == "  " .. p.DisplayName then
                btn.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
                btn.BackgroundTransparency = 0.5
                btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                btn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                btn.BackgroundTransparency = 0.95
                btn.TextColor3 = Color3.fromRGB(200, 200, 200)
            end
        end
    end
end

function notify(msg)
    local notif = Instance.new("Frame")
    notif.Size = UDim2.new(0, 250, 0, 35)
    notif.Position = UDim2.new(0.5, -125, 0, 20)
    notif.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
    notif.BackgroundTransparency = 0.1
    notif.BorderSizePixel = 0
    notif.Parent = gui
    
    Instance.new("UICorner", notif).CornerRadius = UDim.new(0, 17)
    
    local txt = Instance.new("TextLabel")
    txt.Size = UDim2.new(1, 0, 1, 0)
    txt.BackgroundTransparency = 1
    txt.Text = msg
    txt.TextColor3 = Color3.fromRGB(255, 255, 255)
    txt.Font = Enum.Font.GothamSemibold
    txt.TextSize = 12
    txt.Parent = notif
    
    game.Debris:AddItem(notif, 2)
end

function toggleChase()
    local btn = gui.Main.ToggleBtn
    local dot = gui.Main:FindFirstChild("StatusDot", true)
    local statusText = gui.Main:FindFirstChild("StatusText", true)
    local selName = gui.Main:FindFirstChild("SelectedName", true)
    
    if not isActive then
        if not selName or selName.Text == "Chưa chọn" then
            notify("⚠️ Vui lòng chọn mục tiêu!")
            return
        end
        
        local tp = game.Players:FindFirstChild(selName.Text)
        if not tp then
            notify("⚠️ Không tìm thấy người chơi!")
            return
        end
        
        if not tp.Character or not tp.Character:FindFirstChild("HumanoidRootPart") then
            notify("⚠️ Người chơi chưa spawn!")
            return
        end
        
        targetPlayer = tp
        isActive = true
        
        btn.Text = "⏸ TẮT"
        btn.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
        dot.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
        statusText.Text = "Đang bám: " .. tp.DisplayName
        
        notify("✅ Bắt đầu bám sát " .. tp.DisplayName)
        tpLoop()
    else
        isActive = false
        targetPlayer = nil
        
        btn.Text = "▶ KÍCH HOẠT"
        btn.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
        dot.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
        statusText.Text = "Đã dừng"
        
        notify("⏹ Đã dừng bám sát")
    end
end

-- TELEPORT ra sau lưng
function tpLoop()
    task.spawn(function()
        while isActive and targetPlayer do
            local char = Player.Character
            local tChar = targetPlayer.Character
            
            if char and tChar then
                local hrp = char:FindFirstChild("HumanoidRootPart")
                local tHrp = tChar:FindFirstChild("HumanoidRootPart")
                
                if hrp and tHrp then
                    -- Tính vị trí sau lưng target
                    local dist = 5
                    local lv = tHrp.CFrame.LookVector
                    local behindPos = tHrp.Position - (lv * dist)
                    
                    -- TELEPORT
                    hrp.CFrame = CFrame.new(behindPos, tHrp.Position)
                    
                    -- Update status
                    local statusText = gui.Main:FindFirstChild("StatusText", true)
                    if statusText then
                        local d = math.floor((hrp.Position - tHrp.Position).Magnitude)
                        statusText.Text = "Đang bám: " .. targetPlayer.DisplayName .. " (" .. d .. "m)"
                    end
                end
            end
            
            task.wait()
        end
    end)
end

-- F5 toggle
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.F5 then
        gui.Main.Visible = not gui.Main.Visible
        if gui.Main.Visible then
            updatePlayerList()
        end
    end
end)

-- Init
local main = createGUI()
gui = {Main = main}
updatePlayerList()
notify("👁️ Script loaded! F5 to open")
